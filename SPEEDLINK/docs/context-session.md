# Speedlink Black Widow — коррекция осей через vJoy (контекст проекта)

> 🕹️ **Полный контекст проекта/сессии:** диагностика джойстика, коррекция осей и подача исправленного сигнала в vJoy, скрытие физического стика от MSFS.
> _Сгенерировано: 2026-06-05. Обновлено: 2026-06-05 (диагностика по USB-портам, замер дрожания, сброс калибровки реестра, выделение `joy_core.py`, боевой `vjoy_feeder.py`, перевод `joy_diag.py` на общую математику). Пользователь: Evgen Kamenskih._

## 0. Журнал AI-сессии 2026-06-05

Подробный журнал текущей AI-сессии сохранён отдельно:

- [`session-2026-06-05-ai-worklog.md`](./session-2026-06-05-ai-worklog.md)

В нём зафиксированы:

- исходный запрос и уточнения по personal/Windows-only scope;
- архитектурные решения;
- почему появился `joy_core.py`;
- как `joy_diag.py` был переведён на `joy_core.py`;
- что исправлено в edge calibration;
- какие тесты были добавлены и прогнаны;
- PR #1 и PR #2;
- текущее состояние `main`;
- что ещё нужно проверить вручную на Windows.

## 1. Задача

Сделать GUI-программу диагностики манипуляторов и на её основе — программную коррекцию осей джойстика, которая:

1. Определяет все манипуляторы в системе (оси, кнопки, POV, VID/PID).
2. Проводит диагностику и выгружает её в лог (JSON + CSV).
3. По логу применяет коррекцию осей через **vJoy + vjoy_feeder.py**.
4. Работает после переподключения джойстика и перезагрузки ПК.
5. Прячет реальный физический джойстик от **MSFS 2020/2024** (устранение дубля).

## 2. Оборудование

| Параметр | Значение |
| --- | --- |
| Устройство | Speedlink Black Widow SL-6640 (flight stick) |
| Имя в системе | `Microsoft PC-joystick driver`, joy_id 0 |
| VID / PID | `0x07B5` / `0x0317` |
| Оси | X=крен, Y=тангаж, **Z=газ** (абсолютный, без центровки), R=руль/twist |
| Кнопки | 8 |
| POV | есть |
| Диапазон осей | 0–65535 (caps == calibrated) |
| vJoy target | устройство #1 |

## 3. Файлы проекта

| Файл | Назначение |
| --- | --- |
| `joy_diag.py` | **JoyDiag 2.1** — GUI на tkinter + winmm (ctypes). Поиск ВСЕХ манипуляторов (id 0..15), живой просмотр всех 6 осей (X/Y/Z/R/U/V), кнопок и POV. Диагностика покоя, калибровка краёв, выгрузка JSON/CSV, встроенный preview через pyvjoy. Математику берёт из `joy_core.py`. |
| `joy_core.py` | Чистая математика коррекции без Windows-зависимостей: статистика, автогенерация профиля, edge-remap, deadzone, scale, invert, vJoy range, safety для auto-center. Покрыто unit-тестами. |
| `vjoy_feeder.py` | Фоновый фидер: читает физический стик (winmm), применяет коррекцию по профилю через `joy_core.py`, отдаёт в vJoy через `vJoyInterface.dll` напрямую (без pyvjoy). Устойчив к переподключению/перезагрузке. Кнопки и POV 1:1. Автоцентровка при старте и каждом переподключении. |
| `joydiag_profile_final.json` | Профиль коррекции (центр, deadzone, масштабы, тип оси, edge calibration, vJoy target). Читается фидером при старте. |
| `setup_autostart.ps1` | Автозапуск фидера при входе через Планировщик задач (задержка 15с, авто-перезапуск, без окна). |
| `setup_hidhide.ps1` | Скрытие физического стика от MSFS через HidHide (whitelist для python). |
| `tests/test_joy_core.py` | Unit-тесты критичной математики коррекции. |
| `docs/session-2026-06-05-ai-worklog.md` | Полный журнал текущей AI-сессии: решения, PR, проверки, текущее состояние. |

### Лог-файлы (создаются рядом с joy_diag.py)

| Файл | Назначение |
| --- | --- |
| `port_center_log.csv` | Раунды теста переподключения (центр X/Y по портам). |
| `port_center_log_summary.txt` | Сводка/рейтинг портов по центру. |
| `port_jitter_log.csv` | Замеры дрожания (60с, все 6 осей) по портам. |
| `port_jitter_log_summary.txt` | Сводка/рейтинг портов по дрожанию. |
| `vjoy_feeder.log` | Лог фонового фидера. |

> 🔗 Подробные инструкции по отдельным скриптам: [vjoy_feeder.py](./vjoy_feeder-instructions.md) и [joy_diag.py](./joy_diag-instructions.md)

## 3a. Архитектура общей математики

Критичная логика вынесена в `joy_core.py`, чтобы GUI и боевой фидер не расходились:

```text
joy_core.py      = общая математика коррекции
joy_diag.py      = диагностика и preview, использует joy_core.py
vjoy_feeder.py   = боевой фидер, использует joy_core.py
```

Порядок применения профиля:

```text
raw winmm value
  -> norm_axis
  -> edge calibration remap через calibrated_min/calibrated_max
  -> center_offset/deadzone/scale/invert
  -> clamp[-1..1]
  -> vJoy 1..32768
```

Важно: калибровка краёв теперь не только сохраняется в профиль, но реально участвует в коррекции через `joy_core.apply_profile_axis(...)`.

## 3b. JoyDiag 2.1 — детали joy_diag.py

`joy_diag.py` работает со всеми осями манипулятора и использует `joy_core.py` для формул.

### Возможности

- Поиск **ВСЕХ** манипуляторов (id 0..15) и их характеристик (VID/PID, оси, кнопки, POV, диапазоны).
- Живой просмотр всех 6 осей (`X, Y, Z, R, U, V`), кнопок и POV в реальном времени (~100 Гц). Один маркер — сырое значение, второй — с коррекцией.
- Диагностика покоя (8с): центр (mean), шум (min/max/spread), σ, дрейф %, рекомендуемая deadzone.
- **Калибровка краёв** (12с): замер реальных min/max при полном отклонении каждой оси.
- Выгрузка **JSON** (с секцией `correction`) и **CSV** (сырые сэмплы покоя).
- **Запуск коррекции прямо из GUI** — кормит виртуальное vJoy-устройство скорректированными осями (через `pyvjoy`).

### Автогенерация параметров коррекции

- Если `|mean| > 0.9` и `spread < 0.02` → ось считается **нецентрируемой** (газ/слайдер): `type: throttle`, чистый проброс (`scale 1.0`).
- Иначе → `type: stick`:
  - `center_offset = -mean`
  - `deadzone = spread/2 + 2σ + 0.005`;
  - `scale_pos = 1/(1+c)`, `scale_neg = 1/(1-c)`.
- Итоговое применение делается через `joy_core.apply_profile_axis(...)`.

### Маппинг в vJoy и отличие от фидера

- Маппинг осей: `X→X, Y→Y, Z→Z, R→Rx, U→Ry, V→Rz`.
- Выход: `int(round((fixed+1)/2·32767))+1` (1..32768).
- Кнопки пробрасываются в vJoy (до 32).

> ⚠️ **Важно:** встроенная коррекция в `joy_diag.py` использует **pyvjoy** (`pip install pyvjoy` + драйвер vJoy) и **не** пробрасывает POV. Фоновый `vjoy_feeder.py` грузит `vJoyInterface.dll` напрямую (без pyvjoy) и пробрасывает кнопки **и** POV 1:1. Для постоянной работы в игре используется именно фидер; встроенная коррекция в GUI — для быстрой проверки профиля.

## 3c. Диагностика железа «почему уводит» + автоцентровка фидера

### joy_diag.py — раздел «Диагностика железа (почему уводит)»

- **Тест дрожания (60с)** — длинный замер покоя, разделяет причины увода:
  - высокий σ/шум → износ или грязь потенциометра (чистка/замена);
  - стабильный увод оси при малом шуме → механика/калибровка (лечится программно через `center_offset`);
  - заметный дрейф во времени → проседание USB-питания/порта (убрать USB-хаб, попробовать другой порт).
- **Тест переподключения** — снять центр, отключить и снова подключить девайс, нажать кнопку снова. Копит раунды в таблицу и считает разброс центра между ними.
- **Калибровка Windows** — читает из реестра сохранённую Windows-калибровку устройства.
- **Сброс калибровки Windows** — удаляет пользовательскую ветку калибровки (HKCU без админа, HKLM с админом), после чего нужно переподключить USB.

### vjoy_feeder.py — автоцентровка при старте и каждом переподключении

При каждом подключении джоя фидер коротко слушает покой и заново вычисляет реальный центр стика. Это защищает от плавающего центра после переподключений/смены USB-порта.

- **Защиты:** если стик трогают в момент замера или центр подозрительно далёкий — остаётся значение из профиля.
- **Флаги:** `--no-autocenter`, `--autocenter-secs`.

## 3d. Тест переподключения по портам + замер дрожания по портам

В joy_diag.py блок «Тест переподключения (поиск оптимального USB-порта)» реализован классом `ReconnectTest` и расширен замером дрожания по портам.

### Привязка к USB-порту (центр)

- Общее поле **«Метка порта»** + кнопки «Снять раунд», «Сброс таблицы», «Рекомендация».
- **«Снять раунд»:** быстрый замер центра X/Y (~1.5с) под текущей меткой → строка в таблице + лог `port_center_log.csv`.
- **«Рекомендация»:** ранжирует порты по (увод центра + шум + разброс между раундами), помечает оптимальный, пишет `port_center_log_summary.txt`.

### Замер дрожания по порту (все 6 осей)

- Кнопка **«Замер дрожания (60с, все оси)»** — фиксированные 60с.
- **Метка порта — общая** с тестом центра.
- **Лог отдельный:** `port_jitter_log.csv`.
- **«Рекомендация по дрожанию»:** ранжирует порты по средним σ + p2p по всем осям.

### Ветки реестра с калибровкой и сброс

Калибровка джойстика в Windows хранится в двух ветках (в HKCU и/или HKLM):

- `...\\MediaProperties\\PrivateProperties\\Joystick\\OEM\\VID_07B5&PID_0317` (winmm/joyGetPosEx)
- `...\\MediaProperties\\PrivateProperties\\DirectInput\\VID_07B5&PID_0317` (DirectInput)

Полный префикс: `System\\CurrentControlSet\\Control\\MediaProperties\\PrivateProperties\\...`. Сброс = удалить подключ `VID_07B5&PID_0317` + переподключить USB.

## 4. Профиль коррекции (joydiag_profile_final.json)

По последнему чистому замеру (2026-06-05, 524 сэмпла, edge_calibrated):

| Ось | Тип | center_offset | deadzone | scale_pos | scale_neg | Покой (mean) |
| --- | --- | --- | --- | --- | --- | --- |
| X (крен) | stick | −0.368449 | 0.0168 | 1.5834 | 0.7308 | +0.3684 (увод +36.8%) |
| Y (тангаж) | stick | +0.022201 | 0.0194 | 0.9783 | 1.0227 | −0.0222 (−2.2%) |
| Z (газ) | throttle | — | — | 1.0 (проброс) | — | стоял на полном ходу |
| R (руль) | stick | 0.0 | 0.005 | 1.0 | 1.0 | ~0 (идеально) |

`vjoy_target: 1`.

## 5. Правильная последовательность применения

### Этап 0. Подготовка (один раз)

1. Папка `C:\\SPEEDLINK\\`, в неё файлы проекта.
2. Установить: Python, vJoy, HidHide.
3. В vJoyConf включить устройство #1: оси X/Y/Z/Rx, 8 кнопок, POV.

### Этап 1. Диагностика

1. `python joy_diag.py` → выбрать физический Speedlink (VID 0x07B5).
2. «Замерить покой» (8с, руки убрать) → «Калибровка краёв» (12с, все оси до упоров, включая газ) → «Сохр. JSON».
3. JSON → пересобрать `joydiag_profile_final.json`.

### Этап 2. Проверка коррекции

1. `python vjoy_feeder.py`.
2. «Игровые устройства» → vJoy Device → крен в покое ровный, газ/руль на полный ход, кнопки/POV работают.
3. Остановить (Ctrl+C).

### Этап 3. Скрытие от MSFS

1. `setup_hidhide.ps1 -List` → найти `VID_07B5&PID_0317`, скопировать путь.
2. `setup_hidhide.ps1 -DevicePath "HID\\VID_07B5&PID_0317\\..."`.
3. Запустить фидер, проверить `vjoy_feeder.log`.

### Этап 4. Автозапуск

1. `setup_autostart.ps1` → старт сейчас и при каждом входе.

## 6. Как применять профиль

Положить `joydiag_profile_final.json` рядом с `vjoy_feeder.py` — фидер читает его при старте автоматически.

Свой путь можно задать аргументом:

```powershell
python vjoy_feeder.py "C:\\путь\\profile.json"
```

## 7. Кнопка «Загрузить JSON» в joy_diag.py

Подтягивает ранее сохранённый профиль обратно в GUI (диапазоны, коррекция, vJoy target), чтобы запустить preview-коррекцию или посмотреть профиль без повторного замера.

## 8. Известные нюансы и решения

- **SyntaxError: 'utf-8' codec can't decode byte 0xe4** — PowerShell/Notepad сохранил `.py` как ANSI/cp1251. Решение: использовать UTF-8.
- **Y/Z/R «мёртвые» в логе** — часто артефакт: оси не двигали во время 8с покоя.
- **Калибровка краёв == caps [0,65535]** — у этого устройства полный ход может уже совпадать с caps.
- **CSV содержит только фазу покоя** — для коррекции важны центр из покоя и края из калибровки.
- **HidHide ↔ winmm:** фидер читает стик через winmm. Если после cloak в логе «джойстик не найден» — проверить whitelist python/pythonw.

## 9. Сборка .exe (опционально)

```powershell
pip install pyinstaller
pyinstaller --onefile --noconsole joy_diag.py
```

Фидеру `pyvjoy` не нужен — он грузит `vJoyInterface.dll` напрямую.

## 10. Откат

- Скрытие: `setup_hidhide.ps1 -Off`.
- Автозапуск: `setup_autostart.ps1 -Remove`.

## 11. Журнал диагностики портов (2026-06-05)

### Сброс калибровки + тест центра по портам

- Пользователь удалил обе ветки реестра (Joystick/OEM и DirectInput) и переподключил джойстик.
- Снято по 2 замера центра на каждый из 4 портов задней панели (rear1–rear4) → `port_center_log.csv` (8 строк).
- **Результат:** центр стабилен везде. rear2/rear3/rear4 — ровно 0.000000 по X и Y. rear1 — воспроизводимо −0.000015 по X и Y (≈ 1 LSB, пренебрежимо).
- **Вывод:** плавающий центр был из-за сохранённой Windows-калибровки; после сброса реестра проблема ушла. Любой из rear2/3/4 подходит; для страховки оставить автоцентровку фидера.

### Следующий шаг

- Снять замеры дрожания (60с, все 6 осей) на каждом порту → `port_jitter_log.csv` → выбрать самый тихий порт кнопкой «Рекомендация по дрожанию».
